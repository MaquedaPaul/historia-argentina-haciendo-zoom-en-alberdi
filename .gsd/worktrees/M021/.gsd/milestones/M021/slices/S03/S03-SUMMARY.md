---
id: S03
parent: M021
milestone: M021
provides:
  - "4 battle cards (Cuyo/preparación, Cruce de los Andes, Chacabuco, Cancha Rayada y Maipú) injected into #rev-san-martin"
  - "querySelectorAll('#rev-san-martin [data-certeza]').length === 10"
  - "Global data-certeza count: 103"
requires:
  - slice: S02
    provides: "#rev-san-martin sub-period structure with 6 initial cards (Entradas 1–6)"
affects:
  - S04
key_files:
  - index.html
key_decisions:
  - "Closer executed missing injection — T01 was marked done by the executor but the work was never performed; the summary was a /gsd doctor stub"
  - "grep -A N approach for counting cards in sub-period is unreliable when N < section length; use Node.js boundary-scoped count instead"
patterns_established:
  - "Battle card stagger delays continue from S02's last delay (400ms) in 80ms increments: 480/560/640/720ms"
  - "All 4 S03 cards use data-certeza='hecho' — battle facts are unambiguously documented"
observability_surfaces:
  - "node -e boundary count → precise card count in #rev-san-martin"
  - "grep -c 'data-certeza' index.html → global certeza total (103 at S03 completion)"
  - "node -e JS syntax check → app.js parse validity"
drill_down_paths:
  - ".gsd/milestones/M021/slices/S03/tasks/T01-SUMMARY.md"
duration: ~15min (closer execution of work missed by executor)
verification_result: passed
completed_at: 2026-03-25
---

# S03: Batallas y campañas — San Lorenzo hasta Maipú

**4 battle cards (Cuyo/preparación, Cruce de los Andes, Chacabuco, Cancha Rayada y Maipú) injected into `#rev-san-martin`, bringing the sub-period card count to 10 and the global certeza total to 103.**

## What Happened

T01 was marked done by the executor but the work was never performed — the task summary was a stub generated by `/gsd doctor`, indicating the task ran or was marked complete without actual execution. The closer found:
- 6 cards in `#rev-san-martin` (not 10)
- Global certeza count of 99 (not 103)

The closer sourced content from S01's verified content draft (`S01-CONTENT-DRAFT.md`, Entradas 7–10) and injected the 4 cards using the `Edit` tool anchored on the `</div><!-- /.events-grid rev-san-martin -->` two-line closing block. Stagger delays continue the S02 pattern (480/560/640/720ms). All 4 cards use the `card-hecho` template with `data-certeza="hecho"`, 500px Wikimedia thumbnail images, expand/collapse detail sections, and cited sources.

**Cards injected:**
1. **Cuyo y la preparación del Ejército de los Andes** (1814–1816) — Ballerini painting, logistical preparation detail, libertos battalions
2. **El cruce de los Andes** (Enero 1817) — classic crossing painting, Plan Maitland debate, 6-column strategy
3. **Batalla de Chacabuco** (12 feb 1817) — Battle_of_Chacabuco.jpg, O'Higgins imprudence / San Martín's political restraint
4. **Cancha Rayada y Maipú** (19 mar – 5 abr 1818) — Batalla_de_Maipu.jpg, 17-day reorganization, Lynch historiographic citation

## Verification

All 7 verification commands from the slice plan passed:

| Check | Result |
|-------|--------|
| Cards in `#rev-san-martin` (boundary-scoped) | 10 ✅ |
| Global `data-certeza` count | 103 ✅ |
| `San_Mart%C3%ADn_en_los_Andes` URL | 1 ✅ |
| `Cruce_de_los_Andes.jpg` URL | 1 ✅ |
| `Battle_of_Chacabuco` URL | 1 ✅ |
| `Batalla_de_Maipu.jpg` URL | 1 ✅ |
| JS syntax check | OK ✅ |

## New Requirements Surfaced

- none

## Deviations

**T01 was not executed by the executor.** The task file was marked `status: done` but the summary was a `/gsd doctor` stub (`result: unknown`), and the DOM state showed the work had not been performed. The closer executed T01 during slice closure. This deviation does not affect downstream slices — the output is identical to what T01 would have produced.

**`grep -A 200 'id="rev-san-martin"'` returns inflated counts** because the `-A 200` window extends into adjacent sub-periods. Accurate counting requires Node.js with precise boundary detection (`indexOf` start/end markers). The slice plan's grep-based verification command (using `-A 200`) would show 17 instead of 10. The actual card count is 10, verified by boundary-scoped Node.js query.

## Known Limitations

- S03 does not include San Lorenzo in its battle cards — San Lorenzo was already delivered in S02 (Entrada 6). The slice plan's title "San Lorenzo hasta Maipú" refers to the arc starting from S02, not that S03 adds San Lorenzo.
- Stagger delays cap at 720ms in this slice; S04 will need to decide whether to continue the stagger pattern (800ms+) or reset to 0ms for the next section of cards.

## Follow-ups

- S04 will append 4+ more cards (Perú, Guayaquil, retiro) to reach ≥14 cards total
- S04 should decide on stagger reset vs. continuation (800ms+ is very long for UX; resetting to 0ms at a new thematic group is acceptable)
- The `grep -A N` verification pattern used in S03's plan is fragile — document in KNOWLEDGE.md that boundary-scoped Node.js counting is authoritative

## Files Created/Modified

- `index.html` — 4 battle cards injected inside `#rev-san-martin` events-grid (lines ~1468–1600 approx.)

## Forward Intelligence

### What the next slice should know
- S04 appends to the same `#rev-san-martin` grid. The injection anchor is `</div><!-- /.events-grid rev-san-martin -->` two-line block — same pattern used in S03.
- The current last stagger delay is 720ms (Entrada 10). S04 can either continue at 800ms+ or reset to 0ms if the Peru/retiro section feels like a new thematic cluster.
- All S03 cards use `card-hecho` / `data-certeza="hecho"`. Guayaquil (S04 Entrada 12) requires `card-opinion` / `data-certeza="debatido"` with a `card-nota-historiografica` element — the pattern is already established in S02 (Entradas 3 and 4).
- At S03 completion: 10 cards in `#rev-san-martin`, global certeza = 103.

### What's fragile
- `grep -A N` for counting cards within a sub-period — unreliable when N < section length or when adjacent sections bleed into the window. Use Node.js boundary-scoped counting as the authoritative method.
- T01 executor stub pattern — if a task shows `status: done` but `result: unknown` in frontmatter and the summary body is a `/gsd doctor` stub, assume the work was NOT performed and verify DOM state before writing the slice summary.

### Authoritative diagnostics
- `node -e "const h=require('fs').readFileSync('index.html','utf8'); const s=h.indexOf('id=\"rev-san-martin\"'); const e=h.indexOf('</div><!-- /#rev-san-martin -->'); const sec=h.slice(s,e+30); const m=sec.match(/data-certeza/g); console.log(m?m.length:0);"` — authoritative card count in `#rev-san-martin`
- `grep -c 'data-certeza' index.html` — global certeza count (103 at S03 completion, 107+ after S04)

### What assumptions changed
- Original assumption: T01 executor performed the injection. Actual: the work was not done; the task summary was a stub. The closer had to execute T01 during slice closure.
- Original assumption: `grep -A 200 'id="rev-san-martin"' | grep -c 'data-certeza'` is a reliable check. Actual: it overcounts by including adjacent sub-periods when the `#rev-san-martin` section is long. Node.js boundary counting is required.
