---
phase: 01-estructura-y-arco-del-norte
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - styles.css
  - index.html
autonomous: true
requirements:
  - ARCO-02
  - INTG-02

must_haves:
  truths:
    - "User sees 'Alto Peru y la Frontera' as a clickable entry in the sticky sub-nav"
    - "Clicking the sub-nav link scrolls to the new sub-period section"
    - "The sub-period wrapper exists with correct id and title"
    - "Existing .card-nota-historiografica paragraphs site-wide now render as styled callout blocks with left border"
  artifacts:
    - path: "styles.css"
      provides: ".card-nota-historiografica CSS rule"
      contains: ".card-nota-historiografica"
    - path: "index.html"
      provides: "Sub-nav link and sub-period skeleton"
      contains: "rev-alto-peru-guerra-gaucha"
  key_links:
    - from: "index.html sub-nav link"
      to: "index.html sub-period div"
      via: "href=#rev-alto-peru-guerra-gaucha matching id=rev-alto-peru-guerra-gaucha"
      pattern: "rev-alto-peru-guerra-gaucha"
---

<objective>
Add the structural scaffold for the new Alto Peru sub-period: one CSS rule for .card-nota-historiografica callouts, one sub-nav link, and the empty sub-period wrapper with events-grid ready for card insertion.

Purpose: Establishes the container that Plans 02 and 03 will fill with card content. Also retroactively styles ~10 existing .card-nota-historiografica paragraphs site-wide.
Output: styles.css with new rule, index.html with sub-nav entry and empty sub-period skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-estructura-y-arco-del-norte/01-CONTEXT.md
@.planning/phases/01-estructura-y-arco-del-norte/01-RESEARCH.md
@.planning/phases/01-estructura-y-arco-del-norte/01-UI-SPEC.md

<interfaces>
<!-- Executor MUST read styles.css lines 1310-1325 and index.html lines 361-373 and 2344-2360 before editing. -->

From styles.css (insertion point — add AFTER line 1323, BEFORE line 1325):
```css
/* Line 1320-1324 */
.card-rumor:hover {
  border-left-color: var(--certeza-rumor);
  box-shadow: var(--shadow-elevated), inset 0 0 0 1px var(--certeza-rumor-mid);
}
/* NEW RULE GOES HERE */
/* ---- Responsive adjustments for certeza cards ---- */
```

From index.html (sub-nav — insert new link AFTER line 366, BEFORE line 367):
```html
<!-- Line 366 -->
<a href="#rev-1800-1820" class="sub-nav__link">1800–1820<span class="sub-nav__link-label">Revolucion e Independencia</span></a>
<!-- NEW LINK GOES HERE -->
<a href="#rev-1820-1835" class="sub-nav__link">1820–1835<span class="sub-nav__link-label">Anarquia y Guerras Civiles</span></a>
<!-- Line 367 -->
```

From index.html (sub-period insertion — insert AFTER line 2346, BEFORE line 2348):
```html
<!-- Line 2345-2346 -->
          </div><!-- /.events-grid SP1 -->
        </div><!-- /#rev-1800-1820 -->
<!-- NEW SUB-PERIOD GOES HERE -->
        <!-- CONECTOR ALBERDI — SP1 -> SP2 (Pasaje 1) -->
<!-- Line 2348-2349 -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .card-nota-historiografica CSS rule to styles.css</name>
  <files>styles.css</files>
  <read_first>
    - styles.css (full file context around lines 1310-1325 to see exact insertion point)
    - .planning/phases/01-estructura-y-arco-del-norte/01-RESEARCH.md (CSS rule specification at line 186-196)
    - .planning/phases/01-estructura-y-arco-del-norte/01-UI-SPEC.md (New CSS Required section)
  </read_first>
  <action>
Add the following CSS rule block to styles.css AFTER the `.card-rumor:hover` closing brace (after line 1323) and BEFORE the `/* ---- Responsive adjustments for certeza cards ---- */` comment (line 1325). Insert an empty line before and after the new block.

Exact CSS to add:

```css

/* ---------- Nota historiografica callout — inline within card-detail ---------- */
/* Generic class used in card expand panels to flag contested interpretations.
   Retroactively styles existing .card-nota-historiografica paragraphs site-wide. */

.card-nota-historiografica {
  margin-top: var(--space-md);
  padding: var(--space-sm) var(--space-md);
  border-left: 3px solid var(--certeza-opinion);
  background: var(--certeza-opinion-soft);
  font-size: 0.875rem;
  line-height: 1.6;
  color: var(--color-text);
  border-radius: 0 4px 4px 0;
}

```

Do NOT add any new CSS variables. All variables used (--space-md, --space-sm, --certeza-opinion, --certeza-opinion-soft, --color-text) already exist in :root.
Do NOT modify any existing rules.
  </action>
  <verify>
    <automated>bash -c "grep -q 'card-nota-historiografica' c:/Users/gabri/Desktop/historia/styles.css && grep -c 'border-left.*certeza-opinion' c:/Users/gabri/Desktop/historia/styles.css | grep -q '[1-9]' && echo PASS || echo FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "card-nota-historiografica" styles.css returns the new rule block
    - grep "border-left: 3px solid var(--certeza-opinion)" styles.css matches exactly 1 line
    - grep "background: var(--certeza-opinion-soft)" styles.css matches exactly 1 line
    - The rule appears AFTER ".card-rumor:hover" and BEFORE "@media (max-width: 48rem)"
    - No new CSS variables are declared anywhere in styles.css that were not there before
  </acceptance_criteria>
  <done>The .card-nota-historiografica CSS rule is present in styles.css with correct properties, positioned between the certeza hover rules and the responsive media query. Existing unstyled .card-nota-historiografica paragraphs in the page will now render as left-bordered callout blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Add sub-nav link and empty sub-period skeleton to index.html</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 361-373 for sub-nav, lines 2340-2365 for sub-period insertion point)
    - .planning/phases/01-estructura-y-arco-del-norte/01-RESEARCH.md (Pattern 1: Sub-nav Entry, Pattern 2: Sub-period Wrapper)
    - .planning/phases/01-estructura-y-arco-del-norte/01-UI-SPEC.md (Component Inventory sections 1 and 2)
  </read_first>
  <action>
Two insertions in index.html:

**Insertion 1 — Sub-nav link:**
Find the line containing `href="#rev-1800-1820"` (line 366). Insert the following line IMMEDIATELY AFTER it (before the `href="#rev-1820-1835"` line):

```html
          <a href="#rev-alto-peru-guerra-gaucha" class="sub-nav__link">1810–1821<span class="sub-nav__link-label">Alto Peru y la Frontera</span></a>
```

Use the exact same indentation as surrounding `<a>` elements (10 spaces).

**Insertion 2 — Sub-period skeleton:**
Find the line `</div><!-- /#rev-1800-1820 -->` (line 2346). Insert the following block IMMEDIATELY AFTER it, BEFORE the `<!-- CONECTOR ALBERDI` comment (line 2348):

```html

        <!-- ======================================================
             SUB-PERIODO: Alto Peru y la Frontera Norte (1810-1815)
             6 eventos — 5 hecho, 1 opinion
             ====================================================== -->
        <div id="rev-alto-peru-guerra-gaucha" class="sub-period reveal reveal-fade">
          <h3 class="sub-period__title">Alto Peru y la Frontera Norte — Expediciones 1810–1815</h3>
          <div class="events-grid events-grid--certeza" aria-label="Eventos del sub-periodo Alto Peru y la Frontera Norte (1810–1815)">

            <!-- Cards ALTO-01 through ALTO-05 and ARCO-01 will be added by Plans 02 and 03 -->

          </div><!-- /.events-grid alto-peru -->
        </div><!-- /#rev-alto-peru-guerra-gaucha -->

```

Use the same indentation level as the surrounding sub-period blocks (8 spaces for the `<div>`, 10 spaces for the `<h3>` and inner `<div>`).

Do NOT add any JavaScript. Do NOT modify any existing HTML elements.
  </action>
  <verify>
    <automated>bash -c "grep -q 'rev-alto-peru-guerra-gaucha' c:/Users/gabri/Desktop/historia/index.html && grep -q 'Alto Peru y la Frontera' c:/Users/gabri/Desktop/historia/index.html && grep -c 'rev-alto-peru-guerra-gaucha' c:/Users/gabri/Desktop/historia/index.html | grep -q '[2-9]' && echo PASS || echo FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "rev-alto-peru-guerra-gaucha" index.html returns at least 3 matches: the sub-nav href, the div id, and the closing comment
    - grep "Alto Peru y la Frontera" index.html returns the sub-nav link label
    - grep "sub-nav__link.*1810" index.html returns the new sub-nav entry with year 1810-1821
    - The sub-nav link appears AFTER the #rev-1800-1820 link and BEFORE the #rev-1820-1835 link
    - The sub-period div appears AFTER `</div><!-- /#rev-1800-1820 -->` and BEFORE the Alberdi connector blockquote
    - The sub-period contains class="sub-period reveal reveal-fade"
    - The events-grid contains class="events-grid events-grid--certeza"
    - The events-grid has an aria-label attribute
    - No JavaScript was added or modified
  </acceptance_criteria>
  <done>The sub-nav link "1810-1821 Alto Peru y la Frontera" appears in the correct chronological position. The empty sub-period skeleton with id="rev-alto-peru-guerra-gaucha" exists in the correct location within #periodo-revolucion, ready for card content.</done>
</task>

</tasks>

<verification>
1. `grep -q "card-nota-historiografica" styles.css` — CSS rule exists
2. `grep -c "rev-alto-peru-guerra-gaucha" index.html` — returns 3+ (href, id, comment)
3. `grep "sub-nav__link" index.html | grep -A1 "rev-1800-1820"` — new link follows #rev-1800-1820
4. Open index.html in browser — sub-nav shows "Alto Peru y la Frontera", clicking scrolls to empty section
</verification>

<success_criteria>
- styles.css contains the .card-nota-historiografica rule with correct properties
- index.html contains the sub-nav link in correct chronological position
- index.html contains the empty sub-period skeleton at the correct insertion point
- No JavaScript was added or modified
- Existing page functionality is not broken
</success_criteria>

<output>
After completion, create `.planning/phases/01-estructura-y-arco-del-norte/01-01-SUMMARY.md`
</output>
